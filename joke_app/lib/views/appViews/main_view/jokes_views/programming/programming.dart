import 'package:flutter/material.dart';

class ProgrammingView extends StatelessWidget {
  const ProgrammingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}